extends Node

@onready var swords = preload(%Swords)
@onready var axes = preload(%Axes)
@onready var bows = preload(%Bows)
@onready var staves = preload(%Staves)
@onready var hammers = preload(%Hammers)
@onready var arcana = preload(%Arcana)

# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:
	pass
